# Polinema For Nation
Backend web service for polinema for nation book store

## Development

```bash
# install dependency
$ composer install

# migrate database
$ php artisan migrate

# seed database
$ php artisan db:seed

# run server
$ php artisan serve
```
